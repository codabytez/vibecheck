interface IconProps {
  className?: string;
}

export const Equalizer = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    equalizer
  </span>
);

export const AutoAwesome = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    auto_awesome
  </span>
);

export const UploadFile = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    upload_file
  </span>
);

export const Lock = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>lock</span>
);

export const SentimentSatisfied = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    sentiment_satisfied
  </span>
);

export const Schedule = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    schedule
  </span>
);

export const Timer = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>timer</span>
);

export const Forum = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>forum</span>
);

export const CalendarMonth = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    calendar_month
  </span>
);

export const PhoneIphone = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    phone_iphone
  </span>
);

export const Android = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    android
  </span>
);

export const LaptopMac = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    laptop_mac
  </span>
);

export const DesktopWindows = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    desktop_windows
  </span>
);

export const Help = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>help</span>
);

export const FolderZip = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    folder_zip
  </span>
);

export const TextSnippet = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    text_snippet
  </span>
);

export const Security = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    security
  </span>
);

export const Analytics = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    analytics
  </span>
);

export const Close = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>close</span>
);

export const Chat = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>chat</span>
);

export const Lightbulb = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    lightbulb
  </span>
);

export const Favorite = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    favorite
  </span>
);

export const Share = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>share</span>
);

export const TrendingUp = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    trending_up
  </span>
);

export const Check = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>check</span>
);

export const LocalFireDepartment = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    local_fire_department
  </span>
);

export const FileUpload = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    file_upload
  </span>
);

export const WorkspacePremium = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    workspace_premium
  </span>
);

export const Mood = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>mood</span>
);

export const VerifiedUser = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    verified_user
  </span>
);

export const PhotoCamera = ({ className }: IconProps) => (
  <span className={`material-symbols-outlined ${className || ""}`}>
    photo_camera
  </span>
);
